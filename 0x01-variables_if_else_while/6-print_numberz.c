#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int integer;

	for (integer = 0; integer < 10; integer++)
		putchar(integer);
	putchar('\n');
	return (0);
}
